# Array remove duplicates
- 배열에서 중복 체크


```js

//만약 특정 payload 객체를 arr 배열에 추가하는 로직이 있을때, 
//객체의 id 값이 같다면 arr 배열에 추가되지 않게 한다고 가정 했을때,
//filter를 사용해서 간단하게 처리 가능하다.

const arr = [{id: 1}, {id: 2}]

//filter는 중복되는 값을 담은 배열을 반환한다.
const dup = arr.filter((item) => item.id === payload.id).length === 0

if (dups) {
  arr.push(payload)
}

```
